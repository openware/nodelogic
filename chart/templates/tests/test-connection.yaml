apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nodelogic.fullname" . }}-test-connection"
  labels:
{{ include "nodelogic.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "nodelogic.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
